
#ifndef STONECOLD_RESOURCEMANAGER_H
#define STONECOLD_RESOURCEMANAGER_H

#include "Types.hpp"
#include "Exception.hpp"
#include "Texture.hpp"
#include <algorithm>
#include <memory>
#include <string>
#include <vector>
#include <map>

namespace StoneCold::Resources {

class ResourceManager {
//public:
//	ResourceManager(SDL_Renderer* renderer) : _textures(std::unordered_map<hash64, Texture>()), _renderer(renderer) { }
//	ResourceManager(const ResourceManager&) = delete;
//	ResourceManager& operator=(const ResourceManager&) = delete;
//
//	bool LoadResources(const std::vector<Resource>& resources);
//	bool UnloadResources(const std::vector<uint>& resourceIds);
//
//	~ResourceManager() = default;
//private:
//	void LoadTexture(const Resource& resource);
//
//private:
//	std::unordered_map<hash64, Texture> _textures;
//	SDL_Renderer* _renderer;

};

}

#endif
